spring:
  application:
    name: dm-dataservice-query-${spring.profiles.active}
  logging:
    level: info
    appender:
      ref: CONSOLE
    path: logs/${spring.application.name}.log
  cloud:
    nacos:
      discovery:
        server-addr: http://172.21.32.2
        namespace: dam-${spring.profiles.active}
      config:
        server-addr: http://172.21.32.2
        namespace: dam-${spring.profiles.active}
  profiles:
    active: dev