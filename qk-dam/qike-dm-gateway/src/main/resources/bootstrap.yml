server:
  port: 8780
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG
spring:
  application:
    name: dm-gateway
  web:
    locale: zh_CN
  logging:
    level: info
    path: logs/dm-gateway.log
    appender:
      ref: CONSOLE
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: http://172.20.0.13
        namespace: b283864b-1e78-429e-b322-db0f5720f55c
      config:
        server-addr: http://172.20.0.13
        namespace: b283864b-1e78-429e-b322-db0f5720f55c
        extension-configs:
          - dataId: qkdam-gateway-test.yml
            group: DAM
            refresh: true
          - dataId: qkdam-oauth2-resource-test.yml
            group: DAM
            refresh: true
          - dataId: qkdam-redis-test.yml
            group: DAM
            refresh: true
  profiles:
    default: test
