spring:
  application:
    name: ${name}-\${spring.profiles.active}
  logging:
    level: info
    appender:
      ref: CONSOLE
    path: logs/\${spring.application.name}.log
  cloud:
    nacos:
      discovery:
        server-addr: http://172.21.32.2
        namespace: dam-test
      config:
        server-addr: http://172.21.32.2
        namespace: dam-test
        extension-configs:
          - dataId: qkdam-mysql-test.yml
            group: DAM
            refresh: true
  profiles:
    active: test