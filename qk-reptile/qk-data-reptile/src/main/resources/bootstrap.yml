spring:
  application:
    name: dm-reptile
  logging:
    level: info
    appender:
      ref: CONSOLE
    path: logs/dm-reptile.log
  cloud:
    nacos:
      discovery:
        server-addr: http://172.21.32.2
        namespace: dam-dev
      config:
        server-addr: http://172.21.32.2
        namespace: dam-dev
        extension-configs:
          - dataId: qkreptile-mysql-dev.yml
            group: DAM
            refresh: true
          - dataId: qkreptile-oauth2-dev.yml
            group: DAM
            refresh: true
  profiles:
    active: dev